import http.server
import socketserver

PORT = 8080 # Мы открываем нестандартный порт
Handler = http.server.SimpleHTTPRequestHandler

print(f"[*] Сервис запущен на порту {PORT}")
with socketserver.TCPServer(("", PORT), Handler) as httpd:
    httpd.serve_forever(
